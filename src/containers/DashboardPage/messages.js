
import { defineMessages } from 'react-intl';

export const scope = 'scaleui.containers.DashboardPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Dashboard',
  },
  metric: {
    id: `${scope}.metric`,
    defaultMessage: 'Total number of users',
  },
});
