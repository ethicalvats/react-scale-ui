
import { defineMessages } from 'react-intl';

export const scope = 'scaleui.containers.SignUpPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Sign Up',
  },
});
